{
  "name": "server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "@types/node": "^20.10.0"
  }
}